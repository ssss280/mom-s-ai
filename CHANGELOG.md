# 更新日志

本文件记录 ChatSight 的所有重要变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

**两条约定（详见 PLAN.md「更新记录约定」）：**

1. 每次代码或文档改动都必须在这里登记一条，没有例外。
2. **版本号以 GitHub 上的版本为准，提交时取它的下一个**：例如 GitHub 上当前是 1.2.0，本次就提交 1.3.0。
   用 `py release.py` 可以自动查出该用哪个号，并用 `--apply` 同步写进 `version.py`。

## [1.6.0] - 2026-09-21

这一版是**全面功能验收**：把各模块与接口跑一遍、修掉查出来的 bug、统一版本号、清理死代码。
结论与证据都写在下面（没通过的项目也如实列着）。

### 修复

- **配置数值没有校验，会被用户填坏（本轮最严重的 bug）**：设置页允许随便填，
  而 `reply_count` 直接拿去做切片。实测后果：
  - `"abc"` / `3.7` → 模型侧抛 `TypeError`，用户看到「API 调用失败: slice indices must be integers...」；
  - `999` → **真的生成 146 条**推荐回复（白烧 token）；
  - `-1` → `[:-1]` **静默少给一条**（返回 3 条而不是 5 条），完全看不出哪里错了。
  现在新增 `sanitize_config()`：`reply_count` 夹到 1~10、`screenshot_keep` 夹到 0~10000、
  `ocr_method` / `reply_style` 收敛到白名单，**保存时与启动加载时都会收敛**
  （启动时发现脏值会写回并记一条 warning，避免旧脏值一直跟着程序跑）。
- **类型容错不一致**：`screenshot_keep()` 自己有 try/except，`reply_count` 却没有任何保护，
  同一个配置文件里两种风格。现在两者共用同一个收敛函数。

### 变更

- **删除死代码 `capture.capture_region()`**：全仓库无人调用。框选识别走的是
  「服务端截全屏 → 前端拖拽 → `/api/ocr/region` 在服务端裁剪」这条路，
  留着等于有两份裁剪逻辑。函数本身能跑（已实测 300×200 正确），删掉只是去掉重复实现。
- **`PLAN.md` 同步到当前代码**：补上 `local_update.py` / `fair_aliases.py` 两个模块，
  以及查询记录、别名词典、本地更新、配置校验四节；
  原来那句「详见 CHANGELOG 1.5.0」指向的是被重编号的历史条目，已改为按实测来源描述。
- **CHANGELOG 版本号去重**：此前有两组同名版本（2026-09-20 与 2026-09-21 各有一套
  1.5.0/1.4.0/1.3.0/1.2.0/1.1.0）。已把 2026-09-20 与 09-19 那批历史记录重编为
  1.0.1~1.0.5，**内容一字未删**，版本号唯一。

### 验收结果（脚本都在 `.bld/`，可复跑）

| 项目 | 结果 |
| --- | --- |
| 模块功能测试（`test_modules.py`） | **15 通过 / 1 跳过 / 0 失败** |
| 接口测试（`test_api.py`，14 项） | **14/14 通过** |
| OCR 专项（`test_ocr.py`） | **5/5 通过** |
| 前端静态检查（`check_frontend.py`） | 无悬空 id、无未用 CSS、无残留跳转 |

- **通过**：路径解析与可写回退、配置读写、存储 CRUD（建会话/消息/建议/已复制标记/关键词搜索/批量删除）、
  日志落盘与 ERROR 自动进 `error/`、全屏截图（2560×1440，抽样 738 种颜色）、窗口枚举、
  OCR 编码往返与错误提示、五家模型厂商默认配置、搜索 13 个对外函数与查询改写、
  地区+种类别名解析、版本比较与递增、防降级判定、发版版本推算。
- **接口全通**：`/api/config`、`/api/update`、`/api/update/local`、`/api/search/log`、`/api/sessions`、
  `/api/capture`、`/api/windows`、`/api/capture/window`、`/api/ocr/upload`（识别出 22 字）、
  `/api/ocr/region`（4 字）、越界框选被夹回而不报错、`/api/suggestions`（3 条）、
  `/api/chat` + 联网搜索（5 条来源）。
- **跳过（环境限制，非缺陷）**：本机**未安装 Tesseract**，本地 OCR 引擎无法实测
  （走的是视觉模型那一路）。`auto` 模式下视觉失败会正确退回 Tesseract；
  两者都没有时给出"安装 Tesseract / 配视觉 Key"的可读指引（已验证）。
- **已知限制**：`POST /api/capture/window` 对不支持 `PrintWindow` 的程序（实测某个 TFT 窗口）
  返回 400 并提示"改用延时截屏"——这是 Windows 的限制，不是 bug。

## [1.5.0] - 2026-09-21



这一版把"发现新版本"从**跳转 GitHub 网页**改成**直接下载到本地并覆盖**：
点界面上那个「可更新」，程序自己把文件取回来写好，不用再跳出去手工下载解压。

### 新增

- **本地内建更新（`local_update.py`）**：从 GitHub 取回文件并覆盖到程序目录。
  - **下载通道按实测选**：`raw.githubusercontent.com` 在本机**直接超时**（10 秒读不到数据），
    所以没用它。优先 `codeload.github.com` 整包 zip（实测 79KB / 0.8~1.7s，一次拿全），
    失败再退到 `api.github.com` 逐文件下载（0.6s/个）。
  - **只覆盖仓库里真实存在的文件**，本地多出来的文件一律保留不删。
  - **不动用户数据**：`config.json`（含 API Key）、`data/`、`error/`、`.git/`、`.bld/`、
    `build/` 全部跳过；即使远端包里带了 `config.json` 也不会写进来。
  - **覆盖前自动备份**到 `data/update_backup/<时间戳>/`，可手动回滚。
  - **原子替换**：先写 `<文件>.update_tmp` 再 `os.replace`，不会留下写了一半的文件。
- 接口：`GET /api/update/local`（预演）、`POST /api/update/apply`（下载并覆盖）。
- 界面：点版本徽标的「可更新」直接调本地接口，完成后提示"请重启程序"，**不自动重启**。

### 修复

- **防降级护栏（关键）**：实测远端仓库当时停在 **1.3.0**，而本地已经是 **1.4.0**
  （`web_search.py` 本地 73KB、远端只有 27KB）。无条件覆盖会**把本地新代码静默降级冲掉**，
  日志还写着"更新成功"——这种倒退比更新失败危险得多。现在 `local_update.is_downgrade()`
  会在写盘前拦下并返回 `blocked=downgrade`（接口回 HTTP 409），除非显式 `force=True`。
- **计划与下载来自同一份数据**：原先把 `plan()`（问 api.github.com 的 tree）和下载
  （codeload zip）做成两次独立请求，两边不一致时会报出"计划新增 20 个、实际一个没写"这种
  自相矛盾的结果。现在计划直接**基于解包后的真实文件**计算。

### 说明

- 验证方式（都留了脚本，在 `.bld/`）：
  - `test_update_e2e.py`：起本地假更新源 + 沙箱目录，**9/9 通过**
    （覆盖旧文件、落地新文件、API Key/数据库/用户自有文件都不动、有备份、无临时残留）；
  - `test_real_download.py`：打**真实 GitHub 源**，在沙箱里实跑下载+写盘，**7/7 通过**；
  - 防降级护栏单独验证：本地 1.4.0 对远端 1.3.0 → 拒绝，写入文件数 0。
- 更新不会自动重启进程，改完要重启才生效（界面上会提示）。

## [1.4.0] - 2026-09-21



这一版解决"用户说的名字和官方名字对不上"的问题：说「香港照明展」「香港灯展」时，
要么直接命中官方页、要么**先如实说没找到、再指出可能想搜的是「香港国际秋季灯饰展」并给出它的信息**。

### 新增

- **展会别名词典（`fair_aliases.py`）**：按「**地区 + 种类**」两层拆开维护，而不是做一张
  完整短语对照表——用户说法千变万化（香港照明展/香港的灯展/香港照明展览会），
  拆开后只有两个变量，各自加一行就能覆盖大量说法，新增展会也只改一行。
  - 地区别名表（香港/港/香港特别行政区…）、种类别名表（照明/灯饰/灯具/灯光→照明；
    玩具/模型→玩具…）、已知展会表（(地区,类目) → 官方名 + 补充说明 + 同期展会）。
  - **只在"地区与种类都命中、且该组合确有官方展会"时才给建议**，不猜；
    不写日期地点（会过期），那些由检索结果给出。
- **官方名兜底检索**：原说法**没搜到可信结果**时，自动按推断出的官方名再搜一次，
  并把结果当作回答依据。实测这类说法确实搜不到或只搜到泛页：
  `香港灯展` / `香港照明展览会` / `香港灯光展` / `香港文具展`；
  换成官方名（香港国际秋季灯饰展 / 香港国际文具及学习用品展 …）立刻命中官方页。
- **"先说明没找到、再给官方名"的回答顺序**（`SEARCH_ALIAS_SYSTEM_PROMPT`）：
  要求第一句就是「我没找到关于「<用户原话>」的相关内容。」，第二句「你可能想要搜索的是<官方名>」，
  然后才介绍该展会。为此 `search()` 额外返回 `asked`（用户原话）与 `suggestion`
  （地区/类目/官方名/同期展会），界面上也能看到"实际搜索词"变化。
- **跑题判定（类目同义词）**：查询能解析出类目时，结果里**一个类目词都没有**就判为跑题并扣分。
  实测意义：搜「香港玩具展」时百度百科的「香港特别行政区」靠"香港"+零散 bigram 拿到 0.35，
  刚好压在阈值 0.28 之上，于是既没触发官方名兜底、也没被判跑题。

### 修复

- **追问句里的并列新话题不再被硬拼**：用户先问「香港玩具展是什么时候」，再追问「香港照明展呢」，
  旧逻辑把新话题当"附加约束"拼成「香港玩具展是什么时候 香港照明展」——两个话题混成一句，
  削完只剩「香港照明展」，要等换查询词那一轮才可能救回来，实际对话里预算已经耗在别的源上。
  现在只有**自己没带实体**的追问（「2026年的呢」「那地点呢」「还有呢」）才继承上文；
  自带新话题的追问（「香港照明展呢」「新加坡那个呢」）直接自己搜。
- **"泛指向页"字面规则的误伤**：曾经用"标题覆盖查询实体"的字面规则判泛指向页，
  结果把 **HKTDC 官方页也判成泛指向页**——因为用户说「照明」，而官方名写「灯饰」。
  现在改用类目同义词判断（照明/灯饰/灯具都算对题），官方页不再被扣分。
- **追问削短下限**：`还有呢` 这类削完什么都不剩的，直接沿用上一句，不再拼出「… 还有呢」。

### 说明

- 「香港照明展」这类说法**本身就能搜到官方页**，所以官方名兜底只在"没搜到可信结果"时触发，
  不会改写用户原话、也不会平白多打一次引擎（实测该说法首条就是 HKTDC 官方页）。
- 新增 4 道评测题（`category: alias`：香港灯展 / 香港文具展 / 香港照明展 / 香港玩具展），
  题库共 29 题。
- 用户报告的原案例已复现并修复（三次搜索都正常）：`香港玩具展是什么时候`→官方页、
  `香港照明展呢`→官方秋季灯饰展页、`香港秋季灯饰展是什么时候`→官方页。

## [1.3.0] - 2026-09-21



这一版加**后台查询记录**，并把记录当场抓出来的两个搜索 bug 修掉。
起因是"搜出来的东西不对，但说不清哪一步不对"——`data/app.log` 里只有零散的行，
看不出"这次到底用了哪个查询词、打了哪些源、每个源给了什么"。

### 新增

- **后台查询记录（排查用）**：每次联网搜索都会落一条记录到 `data/search_queries.jsonl`（JSON Lines，
  超过 3MB 自动滚动成 `.1`），写明**用了哪个查询词、打了哪些源、每个源什么状态（正常/无结果/被拦/
  失败/冷却跳过/缓存命中）、留下了什么、丢掉了什么**。三种查看方式：
  - 界面：AI 对话页「搜索记录」按钮（可切"只看有问题"、可清空）；
  - 接口：`GET /api/search/log?limit=30`、`POST /api/search/log/clear`；
  - 命令行：`py -3 eval/show_queries.py --detail`（还有 `--problems` / `--only 关键字` / `--clear`）。
  - 记录里把"分低丢弃"和"只是超出 count 名额"分开列，排查时这两件事完全不同；
  - 写记录**任何失败都不影响搜索**（排查日志不该成为新的故障点）。

### 修复

- **查询词改写优先于降级换源**：主循环改成"批次在外、查询词在内"——同一批引擎先把
  「原样 / 去年份 / 去问句」试完，都拿不到才去动更差的引擎。这个顺序是被查询记录直接抓出来的：
  搜「北京天气怎么样」时 `cn.bing` 一律返回「北京市_百度百科」这类泛指向页，
  而把查询词削成「北京天气」后**同一个引擎立刻给真天气页**；旧顺序会把预算耗在后面的差引擎上，
  换词那条路根本走不到。
- **查询词允许削到 2 个字**（原来要求 ≥3）：保留"还剩检索价值"的判断，但不再因为
  削短就整个丢掉「北京天气」这种短查询词。
- **"泛指向页"不再算搜到**：新增 `_is_generic_page` 判定（首页/栏目页、标题只讲实体的一部分、
  实体 bigram 覆盖不足、查询实词全没覆盖），命中扣分；并且当**最相关的那条就是泛指向页**时
  按"没搜到"处理，用来触发换查询词。
- **前端 XSS 防线**：搜索记录的标题/摘要来自外部网页，渲染前统一走新增的 `escapeHtml()`，
  避免把外部 HTML 直接拼进 `innerHTML`。

### 说明

- 前两个 bug 都是**查询记录功能上线后当场抓到的**：搜「北京天气怎么样」返回 `北京市_百度百科`
  却标记为"正常"，日志里 `query_used` 显示用的是带"怎么样"的原词 —— 没有这个记录，
  这个 bug 只会表现为"偶尔答非所问"，很难定位。
- 修复前后对比（同一时段实测）：`query_used` 从 `北京天气怎么样` → `北京天气`，
  首条从「北京市_百度百科」(0.36) → 「北京天气预报…」`weather.com.cn` (1.00)。
- 交替 A/B 复核未引入回退：当前更好 3 / 更差 0 / 持平 22。

## [1.2.0] - 2026-09-21



这一版是**对联网搜索上一轮优化的返工**。上一轮（见下方 1.5.0）用"先跑原始实现、再跑新实现"
的方式对比，得出"总分 43 → 89"的结论；这次改成**交替 A/B**（同一时刻、逐题交替跑两份实现）
重测，发现那个结论**不可复现**，而且新实现在同一时间窗口下明显更差：判据全中 5/25 对 18/25、
中位延迟 12.1s 对 1.2s，首条经常是「2026年放假安排」「北京 _ 百科」这类完全跑题的结果。

### 修复

- **引擎批次顺序完全排错**（上一轮最大的错）：把 `so360` 排到第 4 批（最后）、
  把一个已经失效的源排到第 1 批。新增 `eval/engine_eval.py` 顺序实测（25 题 × 每个源、间隔 0.2s）：

  | 源 | 有贡献的题数 | 每次达标条数 | 被拦 |
  | --- | --- | --- | --- |
  | `so360` | **96%** | 3.00 | 0/25 |
  | `bing_cn` | 40% | 1.40 | 0/25 |
  | `bing_web` | 36% | 1.36 | 0/25 |
  | `bing_news` | **0%** | 0.00 | 0/25 |
  | `sogou` | 0% | 0.00 | 0/25 |
  | `baidu` | 0% | 0.00 | **25/25** |

  现在第 1 批是 `so360` + `bing_cn`，把零贡献的源放最后。**批次只留 2 个源是刻意的**：
  并行猛打会把源打爆——同一时段并行打 8 个源时 `so360` 被拦 23/25。
- **"低分兜底"把垃圾喂给模型**：为了让结果不为空，上一版会把 0.05 分以上的候选塞进上下文。
  实测搜「香港国际秋季灯饰展」时塞进去的是「2026年放假安排」（0.12 分），
  模型会把放假安排当展会时间照抄，用户完全看不出问题。现在**只接受 ≥`FALLBACK_MIN`**，
  再低就明确返回"没查到"。
- **返回条数不受 `count` 约束**：重构时丢掉了截断，线上按 `count=5` 申请却拿到 11~14 条
  （含 0.1 分的百科/攻略噪声），模型上下文被垃圾占满。现在按分数截到 `count`，
  并用相对下限（最高分 × 0.55）挡掉"垫底凑数"的结果。
- **被安全验证拦下的源没有被冷却**：旧代码只记统计、不冷却，于是整轮 25 道题里反复去打
  已知被拦的源，把 12 秒总预算耗光（这是中位延迟 12s 的主因）。现在被拦即冷却 90 秒，
  纯网络失败冷却 30 秒（只对异常生效，"返回 0 条"不触发）。
- **总预算 12s → 8s，单次请求超时 8s → 5s**：一个连不上的源过去能独占 8 秒；
  搜索是"够用就行"的环节，宁可少几条也不要让用户干等十几秒。
- **追问句被削成问句碎片**：「2026年的呢」会派生出「的呢」这种无意义查询词去搜；
  现在只有"削完还剩检索价值（`term_text` 后 ≥2 字）"的候选才会被采用。
- **实体总览页/首页不再压过真正答案**（新增 `_is_generic_page`，扣分而非淘汰）：
  搜「北京中轴线是什么时候列入世界遗产名录的」时，「北京 _ 百科」靠 bigram 命中拿 0.18、
  搜「DeepSeek API 现在什么价格」时「DeepSeek | API Platform」首页挤掉定价页。
  规则刻意保守（路径=首页/栏目页 且 查询里的年份/英文/中文限定词没被覆盖），
  并用 11 条样例做了单测式校验，避免误伤「北京天气」首页、Apple `/iphone/` 落地页这类正确结果。

### 新增

- **`eval/engine_eval.py`**：按引擎实测"有贡献的题数 / 每次达标条数 / 被拦次数 / 延迟"，
  批次顺序由它决定，而不是靠印象。
- **`.bld/ab_compare.py`（交替 A/B）**：同一时间窗口内逐题交替跑"原始实现 vs 新实现"，
  避免"先跑完 A 再跑完 B"把引擎可用性差异误当成代码差异——这正是上一轮得出错误结论的原因。

### 变更

- **英文查询的批次也重排**：第 1 批改 `bing_web` + `duckduckgo`（实测英文技术题只有它们
  给得出 stackoverflow / 官方文档），`so360` 放第 2 批兜底。

### 说明

- **搜索引擎可用性随时在变**，本机实测到 `so360` 会在"25/25 全通"和"25/25 全被拦"之间反复；
  `bing_news` 从"25 题里 17 题达标"退化成"25 题全空"；`duckduckgo`/`wikipedia` 会整段时间连不上。
  所以：**任何单次测量的结论都可能被环境推翻**，本版所有排序结论都注明测量时间与方式，
  跨时间对比一律用交替 A/B。
- **回归结论（交替 A/B，同一时间窗口，25 题）**：累计 7 轮 A/B（含返工前的对照轮）——
  - 返工**前**的实现：逐题 更好 1 / **更差 18** / 持平 6，平均覆盖 0.910 → 0.487，
    延迟中位 1.20s → **12.11s**（这就是"效果更差"的量化证据）；
  - 返工**后**：逐题 更好 **37** / 更差 33 / 持平 105（6 轮合计），平均覆盖 0.492 → 0.484（持平），
    但**空结果均值 7.7 → 3.3（少 57%）**，延迟中位 7.45s → 7.12s。
  - 上游整体退化的时段里百分制总分只有 54~71/100，这是**环境**结果而非代码结果——
    只与同一时刻的原始实现比较才有意义。

## [1.1.0] - 2026-09-21



这一版聚焦**交互体验改进**：历史记录分类展示、每次点击 AI 对话自动开始新对话、批量删除功能。

### 新增

- **历史记录分组展示**：侧边栏历史记录按类型分为「对话记录」和「识别记录」两组，
  分别带小标题，方便快速区分 AI 对话和 OCR 识别结果。
- **批量删除历史记录**：点击「批量管理」进入选择模式，每条记录前出现勾选框，
  支持全选/取消全选，确认后可一次删除多条记录。
- **后端批量删除接口**（`POST /api/sessions/batch-delete`）：一次请求删除多个会话及其关联消息和推荐回复。

### 变更

- **点击「AI 对话」导航自动开始新对话**：不再恢复上次的对话记录，每次点击都从空白开始，
  已保存的对话仍可在左侧历史记录中查看。
- **移除页面加载时自动恢复对话**：刷新页面后不再自动加载上次的 AI 对话，保持干净的初始状态。

## [1.0.5] - 2026-09-20




这一版集中修联网搜索。触发原因是实际使用中的日志：追问句被当成搜索词、问句把相关性打分稀释到阈值以下、
引擎被集体反爬、抓到 4 个字符的正文也照喂给模型。

### 修复

- **追问句被直接拿去搜**：日志里出现过用「2026年的呢」当查询词。现在 `build_query()` 会识别追问
  （句尾"呢/那/还有"、开头"那/那么"、只有年份等），并拼上最近一条有信息量的用户消息：
  `香港灯具展是什么时候` + `2026年的呢` → `香港灯具展是什么时候 2026年`
  （追问里的新约束保留，问句噪声去掉）。没有上下文时保持原样，`北京天气` 这种短查询不会被误拼。
- **问句成分稀释相关性打分**：「香港灯具展是什么时候」拆出 9 个 bigram，其中 4 个是"是什么/时候"这类噪声，
  真结果被压到阈值之下（实测 20 条全判不相关 → 直接回"没查到"）。
  现在打分前先剔除问句词（`term_text()`）：同一批数据真结果 **0.29 → 1.00**，日历垃圾仍是 0.00。
- **一次并行打 4 个引擎，用几次就被集体反爬**：日志里搜狗/百度/360 接连"被安全验证拦下"，
  之后冷却期里几乎没引擎可用。现在**分两批**：先打 Bing + 360（最抗压），没结果才拉搜狗 + 百度；
  冷却时间由 10 分钟缩到 **5 分钟**（实测拦一会儿就放，10 分钟等于把自己饿死）。
- **4 个字符的正文也当正文**：反爬页/纯 JS 页抽出来极短，现在少于 `MIN_PAGE_CHARS`（100 字符）当没抓到，退回用摘要。

### 新增

- **低相关兜底（分两档）**：以前没有一条达到阈值就直接说"没查到"，模型只能凭记忆答。
  现在 `≥0.3` 算可信结果；`0.2~0.3` 作为**低相关参考**交给模型，并注入 `SEARCH_LOW_RELEVANCE_PROMPT`
  （要求开头声明"没有检索到很匹配的资料，以下仅供参考"、涉及日期/价格/地点要提示自行核对）；
  低于 0.2 仍然丢弃。接口返回 `low_relevance` 标记。

### 说明

- 修了一个真 bug：`_score_and_filter()` 内部仍按阈值过滤，导致"低相关"这一档永远是空的、分层形同虚设。
- 回归里先断言样例分数确实落在 `[FALLBACK_MIN, MIN_RELEVANCE)`，再验证兜底路径 ——
  否则构造的样例分数偏高/偏低，测试会给出假结论（本轮踩了两次）。

## [1.0.4] - 2026-09-20




这一版把 ChatSight 从桌面 GUI 重构为网页版，并在网页版基础上补齐了一批功能与修复。
版本号按约定取 GitHub 上版本（1.2.0）的下一个，所以下面这些改动合并在同一个版本里发布。

### 新增

- **直接截取指定窗口（`win_capture.py` + 「截取窗口」按钮）**：网页版此前只会截整个屏幕，浏览器压在最前面时
  截到的就是浏览器自己，根本没法识别别的聊天窗口。现在：
  - `GET /api/windows` 按 z 序列出可见的顶层窗口（标题、尺寸、是否最小化），过滤工具窗口与 UWP「幽灵窗口」；
  - `POST /api/capture/window` 用系统 `PrintWindow` 让窗口自己把画面画到内存位图，
    **窗口被浏览器挡住也能截到**，而且不会把浏览器拍进去；
  - 前端「截取窗口」弹层里点一行即可截取，返回结构与全屏截图完全一致，共用同一个「拖拽框选 → OCR」流程；
  - 只用 `ctypes`（标准库）实现，不引入 `pywin32` 等新依赖；非 Windows 平台返回 `supported: false` 并给出提示；
  - 句柄相关 API 全部显式声明 `argtypes` / `restype`：ctypes 默认把整数句柄当 `c_int`，64 位下会截断。
- **延时截屏**：「截屏识别」按钮旁新增延时下拉（立即 / 3 秒 / 5 秒），选延时后先倒计时，
  给用户时间切到聊天窗口再截全屏；个别程序不支持后台截图时用它兜底。
- **更新检测模块（`version.py` + `update_check.py`）**：打开页面时自动连 GitHub 检查是否有新版本。
  - 新增 `GET /api/update`；`GET /api/config` 一并返回当前版本号。
  - 检测顺序：`contents/version.py(API)` → `contents/CHANGELOG.md(API)` → `raw version.py`
    → `raw CHANGELOG.md` → `releases/latest` → `tags`，前一个明确 404 才试下一个。
  - **绝不拖慢页面**：接口立即返回已知结果，联网检测放后台线程；前端发现 `pending` 就每 1.5 秒再问一次。
  - **硬时间预算**：`urllib` 的 `timeout` 只约束单次 socket 读，遇到"慢速吐数据"会失效
    （实测 `raw.githubusercontent.com` 拖到 16 秒），因此每个地址都放子线程 `join(剩余预算)`，整轮上限 8 秒。
  - 结果缓存：成功 10 分钟、失败 5 分钟；版本比较按数字段（`1.10.0 > 1.9.9`）；
    CHANGELOG 只认 `## [x.y.z]` 标题行（否则会把正文里 Keep a Changelog 的链接版本 `1.1.0` 当成项目版本，实测踩过）。
- **左下角版本标识**：状态栏最左侧显示 `v1.3.0`。
  - 检测到新版本 → 版本号后面挂一个琥珀色「**可更新**」标记，点击跳转 GitHub；
  - 已是最新 / **连不上 GitHub** / 正在检测 → 只显示版本名称（失败原因放在悬停提示里）。
- **截图保留数量可配置**：设置页新增「截图」区块，配置项 `screenshot_keep`。
  **0（默认）表示永久保留、一张都不删**，填正数才只保留最近这么多张。
- **识别结果与历史记录里查看已保存的截图**：识别结果下方新增缩略图带（**原图** + **裁剪图**）；
  加载历史会话时列出该会话保存过的截图（最多 12 张，标签是识别时间）；点缩略图打开大图弹层，
  可「在新标签打开」或右键另存，`Esc` / 点空白处关闭。
- **联网搜索（`web_search.py`）**：AI 对话页输入框下方新增「联网搜索」开关，勾上后先联网检索，
  再让模型依据检索结果作答、用 `[1] [2]` 标注引用，回答下方列出可点击的来源链接。
  - **不需要任何搜索 API Key**：**并行**查 Bing / 360 / 搜狗 / 百度，合并去重后**按相关性排序**，
    DuckDuckGo 只在前几个都没结果时兜底。
    早期版本是"谁先出结果用谁"，被真实案例打脸：搜「2026香港秋季照明展」时 Bing 返回一堆
    「2026 年日历/放假安排」，它"有结果"就把真搜到展会时间的搜狗结果挤掉了。
  - **相关性过滤**：中文按 2 字滑窗、英文数字按词，命中率低于 0.3 的结果直接丢掉，
    宁可返回空也不把无关网页喂给模型。
  - **反爬识别与冷却**：搜狗/百度很容易触发安全验证（连续查询十几次就被拦），
    识别到验证页后该引擎冷却 10 分钟，不再白撞；360 明显更抗压，实测补位成功。
  - **跳转链接还原**：Bing 的 `/ck/a?...&u=a1<base64>`、360/百度的 `/link?` 跳转页都还原成真实地址，
    这样既能按域名过滤图片/视频垂直页，也让模型和用户看到真实来源。
  - **来源标明网站**：回答下方不再是干巴巴的链接列表，而是「信息来源」区块，
    每条按 `[序号] 网站域名 文章标题` 排列（域名带蓝色底标、标题可点开原文、悬停显示完整 URL）；
    模型拿到的上下文里也带 `来源网站: xxx.com`，但要求它**不要在正文里输出 URL**（界面已经展示了）。
  - **查询词清洗与改写**：聊天里的一句话先砍成第一个分句（`2026香港秋季照明展，什么时候在哪办？` →
    `2026香港秋季照明展`）；首轮没有相关结果时自动换词重试（原样 → 去掉 4 位年份 → 去掉"帮我查一下"这类成分）。
    依据：实测搜 `2026香港秋季照明展` 时 **Bing 把 2026 当实体、返回全年日历**，
    去掉年份后第一条就是 HKTDC 官网；而加 `mkt`、完整请求头、带 Cookie、换 `cn.bing.com` 四种方式结果完全一样，
    说明问题在查询词而不在请求方式。结果里会记录实际生效的查询词，界面显示"改用「X」"。
  - 结果按 query 缓存 5 分钟；**一条都没搜到时会明确告诉模型"没查到"**，
    避免它凭记忆自信作答（实测会把展会地点说错）。
  - **抓取网页正文**：摘要常常只是导航文字（搜天气时摘要全是"XX天气网为您提供…"），
    所以搜完后**并行抓前 3 篇正文**，连同摘要一起喂给模型。实测答案里因此出现了摘要里没有的细节
    （展览面积 6.78 万㎡、门票参考价、完整场馆地址"香港湾仔区博览道1号"）。
    单页超时 5 秒、总共 6 秒、每篇最多 2000 字符、单页最多下载 800KB，慢了就放弃退回摘要；
    设置页新增「读取网页正文」开关（配置 `search_read_pages`，默认开），关掉可省 1~3 秒。
  - 搜索结果的搜索引擎跳转链接（`/link?`）统一在 `search()` 收尾时并行还原成真实地址，
    这样来源显示真实域名，抓正文也不会浪费名额（实测还原前 3 篇只抓到 1 篇，还原后 3/3 全中）。
  - 文本模型若本身走阿里云百炼（DashScope），则改用**模型原生联网搜索**（`extra_body={"enable_search": True}`），
    不再自己抓网页；开启搜索时温度降到 0.3，减少发挥。
- **`安装.bat`：新电脑一键装环境**。自动找 Python（要求 3.9+，跳过微软商店的假 `python` 别名），
  没找到就静默下载安装官方安装包（**仅当前用户，不需要管理员权限**；官方站点不通自动切华为云镜像），
  再用 pip 装依赖（默认源失败自动切清华镜像，装完 import 一遍确认）并启动程序；`安装.bat --no-start` 只装环境不启动。
  改这个脚本必须守三条：**纯 ASCII 内容**（中文 + cmd 代码页切换会让批处理读错位，1.1.0 的 `启动.bat` 踩过）、
  **全部 CRLF 换行**（LF-only 会让 `goto` / 标签解析出错）、**`if (...)` 块内的引号里不能出现 `)`**；
  另外解释器路径与参数必须分开存 —— `set "PY=py -3"` 之后用 `"%PY%"` 调用，cmd 会把 `"py -3"` 整体当成程序名而报找不到。
- 接口补充图片信息：`/api/ocr/upload` 返回 `image_url`；`/api/ocr/region` 额外返回
  `source_url` / `source_path`（未裁剪的原图）；`/api/sessions/<id>` 每条消息带
  `image_url` 与 `image_exists`（文件已不在时为 false，前端自动跳过）。
- **历史记录区分「对话记录」与「识别记录」，AI 对话也存进历史**：以前只有 OCR 识别的结果会入库，
  AI 对话关掉页面就没了。现在左侧历史里两类记录并存，标题分别是 `对话记录 2026-09-20 17:03` 和
  `识别记录 2026-09-20 13:18`。
  - `sessions` 表新增 `session_type`（`chat` / `recognition`），`messages` 表新增 `role`（`user` / `assistant`）；
    **老库启动时自动补列**（`PRAGMA table_info` 查缺再 `ALTER TABLE`），并把旧的「会话 时间」标题就地
    改成「识别记录 时间」，不用手工迁移。
  - `/api/chat` 每次问答后把「最后一条用户消息 + 本次回复」写进对话记录，返回 `session_id`；
    前端带上它继续问就追加到同一条记录。**只存这两条**：前端每次要发完整历史给模型，全存会重复记账。
    落库失败只记日志、不回滚，避免把已经拿到的（要花钱的）回复丢掉。
  - 点历史条目自动分流：对话记录回到「AI 对话」页按原角色回放，并接上上下文可以接着聊；
    识别记录仍回「识别」页。刷新页面会把上次那段对话接回来（记录被删掉则当作新对话，不报错）。
  - 「清空对话」只结束当前这段（下次发送新建一条记录），已保存的记录留在历史里；
    删掉正在进行的记录时同步重置本地记录 id。

### 修复

- **打开页面后整个界面被截屏弹层遮住，什么都点不了**：`#crop-modal` 上的 `hidden` 属性被作者样式
  `display: flex` 覆盖（浏览器 UA 样式表里的 `[hidden] { display: none }` 没有 `!important`，作者样式优先级更高），
  弹层一进页面就全屏显示，点"取消"也关不掉。
  `style.css` 补上 `[hidden] { display: none !important; }` 兜底（同时保护 `#toast`、`#crop-rect`、`#file-input`）。
- **框选时鼠标在"舞台"外松开就静默失效**：`mousemove` / `mouseup` 原本挂在 `#crop-stage` 上，在顶部工具栏或窗口外
  松开鼠标时拖拽状态卡在 `true`，且不会发起 `/api/ocr/region`；选区小于 10px 时也是无声返回。
  现改为挂在 `document` 上，选区过小时提示"选区太小，请重新拖拽框选"，并让 `mousedown` 只在已有截图时生效。
- **启动第二个实例导致"改了代码却不生效、接口莫名 404"**：Windows 的 `SO_REUSEADDR` 允许两个进程同时绑同一端口，
  旧实例没关掉时，新进程照样打印 "Running on http://127.0.0.1:5050"，但浏览器的请求会落到**旧进程**上，
  而旧进程没有新加的路由（本次「截取窗口」失败就是 `GET /api/windows` 404 引起的）。
  现在启动前用 `port_in_use()` 探测端口，被占用时给出可读提示并以退出码 1 结束。
- **`/api/ocr/region` 参数与边界**：文件名为空时 `Image.open()` 会把目录当文件打开并抛 `IsADirectoryError`（500）、
  截图不存在也报 500；前端按缩放比例换算后越界 1~2 像素的选区会被 PIL 用黑边补齐，反过来干扰 OCR。
  现在统一返回 400 明确提示，并把选区夹回图片范围。
- **加载历史会话后左侧列表不高亮**：`loadSession()` 结尾会把所有 `.history-item` 的 `selected` 类删掉，
  改为重新渲染历史列表，让当前会话保持高亮。
- **`analyze_chat_with_vision()` 用错客户端**：检查的是文本客户端、调用的也是 `self.client`，
  配了"DeepSeek 文本 + 千问视觉"时会走错服务商。改为 `is_vision_configured` / `self.vision_client`。
- **启动初始化阶段没有任何日志**：`setup_logging()` 原本只在 `main()` 里调用，而配置 / 模型 / 数据库是模块级初始化，
  一旦初始化就崩会完全没有日志可查。现在提前到模块级初始化之前。
- **对话失败后消息残留在上下文**：`sendChat()` 请求失败时用户消息仍留在 `state.chat` 里，
  重试会发出两条连续的 user 消息，部分 OpenAI 兼容服务会直接报错。现在失败时撤回该条。
- **`--port` 后面不跟端口号会 `IndexError`**：新增 `parse_port()`，参数缺失或非法时回退 5050 并记一条告警。
- **`provider=custom` 且 Base URL 留空会构造出坏客户端**：以前把 `base_url=""` 直接传给 `OpenAI()`，
  请求时的报错完全看不出原因；现在回退到 SDK 默认地址并写告警。

### 变更

- **整体重构为网页版**：新增 `server.py`（Flask 后端，提供配置 / 对话 / OCR / 截屏 / 推荐回复 / 历史会话接口）
  与 `static/`（原生 HTML + CSS + JS 前端），删除 `gui.py`、`main.py`、`setup.py`、`build.bat`、`build_safe.py`
  等 tkinter 与 cx_Freeze 打包链路。
- **截屏方式改为服务端截图 + 浏览器内框选**：`POST /api/capture` 返回全屏截图，
  前端弹层里拖拽框选后按 `截图宽度 / 显示宽度` 换算坐标，再调 `POST /api/ocr/region` 裁剪识别。
- `paths.py` 精简为源码目录解析，不再支持 `CHATSIGHT_HOME` 环境变量与 exe 冻结模式；
  `capture.py` 移除 tkinter 覆盖层相关代码；`requirements.txt` 去掉 `pynput`、加入 `flask`；
  `启动.bat` 改为直接运行 `py -3 server.py` 并打开浏览器。
- 前端为黑白极简边栏布局，保留 AI 对话、识别、历史记录、设置全部功能；识别页按钮区支持换行（`flex-wrap`）。
- **对话上下文限制**：`/api/chat` 只发送最近 30 条消息（`MAX_CHAT_MESSAGES`），并校验 `messages` 结构、
  过滤空内容；模型返回空内容时返回 502 而不是把 `undefined` 显示到界面。
- **截图默认永久保留**：`capture.prune_screenshots()` 的 `keep <= 0`（默认）表示不清理，只有用户填了正数才清理旧图。
- `setStatus()` 改为只写状态文字（`#status-text`），不再整块覆盖状态栏，否则会把版本标识冲掉。
- 配置文件移除已失效的 `language` / `hotkey` 字段（网页版没有全局热键，Tesseract 语言在 `ocr.py` 参数里指定）。
- 文档同步：`PLAN.md` 重写为网页版架构，删除已不存在的 `main.py` / `gui.py` / `CHATSIGHT_HOME` / exe 打包说明，
  补充新模块、新接口、三种截屏方式与「更新记录约定」。

### 说明

- 1.2.0 的 tkinter 截屏覆盖层（整屏压暗、选区保持原色）随 GUI 一并移除，交互由网页弹层替代。
- 部分程序（尤其带硬件加速渲染的）会拒绝后台渲染，此时截取窗口会返回可读的失败提示，改用「延时截屏」即可；
  窗口若已最小化，请先还原再截取。
- `data/screenshots/` 里的文件名带来源前缀（`cap_` 全屏、`win_` 窗口、`up_` 上传、`crop_` 裁剪）与时间戳，可自行整理。
- 遇到"新加的接口 404 / 改动不生效"时，先执行 `netstat -ano | findstr :5050`：
  如果有多行 `LISTENING` 说明跑着多个实例，关掉旧的那个即可（`taskkill /PID <旧PID> /F`）。
- 本机实测：`api.github.com` 0.6~1.2 秒返回，`raw.githubusercontent.com` 同一时刻要 16 秒并被超时掐掉，
  所以更新检测的地址顺序里 API 排在最前面。

## [1.0.3] - 2026-09-20




这一版是**先建评测、再按数据优化**联网搜索：新增 `eval/` 随机测试集与评分脚本，
在 25 道题上把总分从 **43.1 拉到 85~95**（空结果率 60% → 0%），
过程中发现并修掉了几个"看起来在搜、其实在喂假数据"的严重问题。

### 新增

- **联网搜索评测体系（`eval/`）**：
  - `eval/cases_bank.json`：25 道随机测试题库，每题带**客观判据**（关键词分组 + 期望域名），
    覆盖事实/时效/价格/技术/长尾/追问/对抗错误前提等类型。
  - `eval/harness.py`：随机抽题（`--seed` 可复现）→ 跑真实 `search()` → 抓正文 → 按判据评分
    （判据全中率、覆盖度、空结果率、首条命中率、延迟、链接可打开率、正文抓取率、来源多样性），
    并把 `testset.json` / `records.json` / `links.json` / `metrics.json` 落盘到 `eval/runs/<名字>/`。
  - `eval/tuning/build_labels.py` + `tune_scoring.py`：标注数据集（359 条结果，含 8 条
    "权威域但跑题"的对抗负样本），用来给打分函数调参、算 AUC/精确率/召回率。
  - `eval/compare.py`：把多次运行的指标并排对比，出"优化前/后"结论。
- **`eval/check_engines.py`**：用 `web_search.engine_stats()` 巡检各搜索源健康度
  （成功/空手/被拦/失败/累计结果/平均耗时）。
- **Bing 资讯 RSS 搜索源**（`bing_news`）：官方接口，不受诱饵页影响，
  时效/地点/价格类问题比网页搜索准得多。
- **英文维基补充**：中文维基命中不足 2 条时自动补一次英文维基（英文技术词条在那边）。
- **低相关兜底加强**：连 0.15 都不到的候选，也会挑分数最高的一两条交给模型（标注为"相关性较低"），
  而不是直接回"没查到"。

### 修复

- **`www.bing.com` 给爬虫返回整页"诱饵结果"**（最严重）：搜「香港国际秋季灯饰展 2026」返回
  Lady Gaga / Microsoft Support，搜「ChatSight」返回一堆物联网文章——页面结构完全正常
  （10 个合法 `<li class="b_algo">`），所以"解析成功"并不等于"搜对了"，模型会拿到一页假事实。
  现在主力入口换成 `cn.bing.com` + `mkt=zh-CN`，`www.bing.com` 只当最后兜底。
- **追问句被削成空查询词**：`query_variants()` 反复削问句成分时，「git 怎么撤销最近一次提交但保留改动」
  会被削成空串，空查询词打引擎纯属浪费预算。现在过滤掉长度 < 2 的候选。
- **引擎冷却把自己饿死**：旧版按引擎冷却 5 分钟，一次搜歪就让后面几分钟全部没引擎可用。
  现在批次冷却只活在**单次 `search()` 内**（局部状态），不会跨查询累积。
- **传输层失败的引擎被反复重试**：实测 DuckDuckGo 网络不通时整轮被调用 10 次、全部超时
  （每次白等 0.8~1.3 秒）。现在 HTTP 层失败会给自己记 30 秒短冷却；只对**异常**生效，
  "返回 0 条"不触发，避免把正常引擎禁用。

### 变更

- **搜索引擎重新分批**（按实测抗压程度，前一批不够数才升级）：
  1) `bing_cn` + `bing_news`；2) `duckduckgo` + `wikipedia`（英文技术题真正的主力）；
  3) `sogou` + `baidu`（中文好但几分钟就触发验证码）；4) `bing_web` + `so360`（会返诱饵页/验证页）。
- **英文查询换一套批次顺序**：实测 `cn.bing` 对纯英文技术问题**根本不看查询词**
  （搜 asyncio 用法返回 Python 官网首页、搜 docker healthcheck 返回 Docker 官网首页），
  而 stackoverflow / 官方文档只有 DuckDuckGo 和英文维基给得出来。
- **升级条件更严格**：批次够数（`GOOD_ENOUGH=3`）但最高分 < `STRONG_SCORE=0.45`，
  说明只是"沾边"而不是答案，会继续升级找更对的结果。
- **跨批次按分数取最优**，而不是"后一批直接覆盖前一批"：后者会把第 1 批里勉强可用的结果挤掉
  （实测英文题判据 3/3 → 1/3）。
- **相关性打分加了两个"来源是否对味"的信号**：查询词出现在域名里（+0.10）、
  权威站点（政府/百科/官方文档，+0.06）。
- `MIN_RELEVANCE` 0.3 → 0.28、`FALLBACK_MIN` 0.2 → 0.15（标注集上 F1 更优）。
- 结果去重增加"解析后的标题再次去重"。

### 说明

- **试过并放弃：IDF 加权打分**。想法是把"下载"这种到处都是的词压低，让 python.org 官方页浮上来。
  实测在标注集上召回 0.905 → 0.611、端到端判据全中率 0.84 → 0.76、延迟 p50 0.93s → 1.72s
  （低分结果变多 → 动不动就升级打更多引擎），因此没有采用，只在代码注释里留了结论。
- **实测稳定性**：优化后在完整 25 题上连跑 4 次，总分 85.3 / 87.5 / 89.1 / **95.1**（均值 ≈89），
  基线 43.1 分。波动全部来自 DuckDuckGo 时通时不通（同一时段可能连续失败）；
  它的可用与否能把英文题判据从 1/3 拉到 3/3。
- 评测脚本每道题之间**不能**反复清引擎统计，否则看不出"哪个源还能用"；
  状态清理只在整轮开始做一次。

## [1.0.2] - 2026-09-19




### 新增

- **截屏覆盖层改为经典截图交互：整屏压暗、框选区域保持原色**。
  覆盖层背景为压暗 45% 的全屏截图（`capture.DIM_FACTOR`），拖动时把选区内的**原色**画面实时贴回，
  选区外始终是暗的，方便对准区域。原色区域刷新做了 30ms 节流（`capture.BRIGHT_REFRESH_INTERVAL`），
  大范围拖动也不会卡顿。
- 选区边框加粗到 3px，便于在压暗背景上辨认。

### 说明

- 压暗只影响覆盖层的观感；真正送去识别的截图始终取自**未压暗的原图**，不会影响 OCR 效果。

## [1.0.1] - 2026-09-19




本次集中修复了打包运行、日志落盘与截屏识别链路上的多个阻断性问题，并新增了视觉 OCR 的独立配置能力。

### 修复

- **打包后一启动就崩溃**：cx_Freeze 冻结后 `__file__` 指向 `lib\library.zip\logger.pyc`，导致 `data/`、`error/` 目录被算到压缩包内部，`os.makedirs()` 抛
  `FileNotFoundError [WinError 3]`。由于崩溃发生在日志 handler 安装之前，表现为"弹窗报错但一个日志都没有"。
  新增 `paths.py` 统一解析运行目录（`CHATSIGHT_HOME` 环境变量 → exe 所在目录 → 源码目录），所有模块改用它。
- **错误日志不落盘**：`setup_logging()` 加异常保护并返回实际日志路径；新增 `write_crash_log()` 作为最后兜底；
  `main.py` 补上 `threading.excepthook` 与入口级保护；`fatal_error()` 弹窗现在显示真实日志路径。
- **界面回调异常被吞掉**：tkinter 按钮等回调抛出的异常不走 `sys.excepthook`，此前只在 stderr 闪一下。
  现已通过 `report_callback_exception` 写入 `error/` 并提示用户。
- **10 处 `lambda` 闭包引用 `except ... as e` 的 `e`**：Python 会在 `except` 块结束时删除该变量，
  延迟执行的 lambda 抛 `NameError: cannot access free variable 'e'`，把真正的错误信息吃掉了。
- **截屏报 `TclError: image "pyimage1" doesn't exist`**：`ImageTk.PhotoImage` 未指定 `master`，
  图片被注册到主界面的 Tcl 解释器，而选区 canvas 属于另一个解释器。现已显式传入 `master`。
- **点截图后程序"消失"（其实没退出）**：`select_region()` 用第二个 `Tk()` 开嵌套 `mainloop()`，
  而 Tcl 的 `Tk_GetNumMainWindows()` 统计的是**整个线程**的主窗口数，主界面窗口一直存在，
  嵌套 mainloop 永不返回，后面恢复主窗口的代码永远执行不到。改为使用主窗口同一解释器的 `Toplevel` 并不再阻塞调用方。
- **框选区域不准确、后面的字识别不出来**：原先先恢复主窗口、再启动抓屏线程，导致自家窗口弹回置顶后盖住框选区域，
  只有窗口左边缘外的一条缝能识别到。现改为从覆盖层显示的那张全屏截图裁剪（抓屏在覆盖层出现前完成），
  既消除竞态，也做到所见即所得。
- **`启动.bat` 被 cmd 解析错位**：UTF-8 中文文本 + 中途 `chcp 65001` 会让 cmd 读错位，
  导致 `start` 那一行不执行。现已改为纯 ASCII，并通过 `CHATSIGHT_HOME` 让 `data/`、`error/`、`config.json` 落在项目目录。

### 新增

- **文本模型与视觉 OCR 可分别配置**：新增 `vision_provider` / `vision_api_key` / `vision_base_url`，
  留空自动回退到文本配置。设置面板新增"视觉 OCR 独立配置"区块，可让文本走 DeepSeek、OCR 走千问。
- **支持通义千问视觉模型 OCR**（默认 `qwen3-vl-plus`），模型列表补充 `qwen3-vl-flash`、
  `qwen-vl-max`、`qwen-vl-plus`、`qwen-vl-ocr-latest`。
- `build_safe.py`：构建入口，规避 cx_Freeze 在 Python 3.14 上扫描字节码时的随机崩溃
  （`TypeError: unsupported operand type(s) for -: 'range_iterator' and 'int'` / `0xC0000005` 访问违例）。
- `config.example.json`：配置模板（`config.json` 含 API Key，已被 `.gitignore` 排除）。
- `.gitignore`：排除运行期数据、打包产物与本地配置。

### 变更

- OCR 提示词改为强调"完整、逐行、不遗漏最右/最下、不总结不翻译"，`max_tokens` 由 2000 提到 4000，
  并自动去除模型返回的 ``` 代码块包裹。
- `auto` 模式在视觉模型失败时会带上真实失败原因，不再只报"没有可用的 OCR 引擎"。
- `build.bat` 改为纯 ASCII 并调用 `build_safe.py`。
- 数据库路径、截图目录统一由 `paths.py` 解析。

### 说明

- Python 3.14 + cx_Freeze 的字节码扫描崩坏在 `dis._get_cache_size()`（会返回非 int），
  `build_safe.py` 在导入 cx_Freeze 之前对其做了保护；该崩溃具有随机性，构建失败时重试通常即可通过。

## [1.0.0] - 2026-09-19



首个可用版本。

### 新增

- **屏幕区域捕获**：全屏覆盖层拖拽框选（`mss` + `tkinter` Canvas）。
- **OCR 文字识别**：本地 Tesseract 与 AI 视觉模型两种方式，支持 `auto` 自动选择。
- **AI 智能回复推荐**：根据识别到的聊天内容生成多条推荐回复，可一键复制。
- **聊天记录保存**：SQLite 本地持久化会话、消息与推荐回复。
- **多模型支持**：OpenAI、Anthropic、DeepSeek、通义千问、Ollama 及任意 OpenAI 兼容接口。
- **图形界面**：AI 对话、聊天识别、截图 OCR、历史记录、设置五个标签页。
- **日志系统**：`data/app.log` 滚动日志 + `error/` 错误日志目录。
- **打包与启动**：`build.bat` 构建 cx_Freeze 单文件目录版，`启动.bat` 一键运行。
