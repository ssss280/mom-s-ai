# AGENTS.md —— 给 AI 助手 / 贡献者的工作规则

> **任何 AI 助手（Claude / Copilot / Cursor / DeepSeek / 其它）在修改本项目之前，先读完本文件。**
> 这些规则不是建议，是**必须遵守的工作流**。违反其中任何一条都算改坏项目。

---

## 🚫 第一条（最高优先级）：改动版本号前必须先问人

**不要自己决定版本号。** 无论你是要发版、要登记 CHANGELOG、还是顺手改 `version.py`，
**都必须先向用户提出请求并等到明确同意**，才能动版本号。

具体来说，以下行为**一律禁止**在未获同意时执行：

| 禁止 | 说明 |
| --- | --- |
| 修改 `version.py` 的 `__version__` | 除非用户明确同意了这个具体版本号 |
| 新增/重命名 `CHANGELOG.md` 里的版本标题 | 版本条目必须与用户同意的版本号一致 |
| 执行 `py release.py --apply` / `--release` | 这两个命令会改版本号，需先获同意 |
| `git tag` / `git push` 版本标签 | 标签一旦推送到公开仓库，其他人就能看到 |
| 重新编号历史版本 | 会改写已公开的历史，必须逐次确认 |

**正确做法**（先请示，再动手）：

```
用户：帮我修一下搜索的某个 bug
AI  ：（改代码、跑测试）
      "这个改动要不要发版？如果要，我建议 1.1.1（修订号，只含修复）；
       也可以等攒够功能再发 1.2.0。你定，同意后我再改 version.py。"
用户：就 1.1.1 吧
AI  ：（此时才执行 py release.py --approved 1.1.1 ...）
```

**为什么这条要放在最前面**：版本号是对外承诺，改错会影响所有已安装用户的更新判断，
而且一旦推送到公开仓库就收不回来了。这个项目**已经因为自动跳版本号踩过坑**
（早期出现过两组同名版本，见 CHANGELOG「版本编号说明」）。

---

## 第二条：CHANGELOG 必须登记

- **任何代码或文档改动，都要在 `CHANGELOG.md` 里登记一条**，没有例外。
- 分类用 `新增 / 修复 / 变更 / 说明`（遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)）。
- `CHANGELOG.md` 顶部版本必须等于 `version.py` 的 `__version__`，不一致算失败。
- **登记内容时也要遵守第一条**：如果这次改动需要新版本号，先问用户。

## 第三条：不要自作主张"统一版本号"

看到版本号看起来"乱"或"跳号"时，**不要直接动手重排**——先说明现状并询问。
版本编号涉及已发布的历史与 Git 标签，用户可能有意为之。

## 第四条：改动后的最小验证

改完至少跑一遍相关自检（脚本在 `.bld/`，未纳入版本控制）：

```powershell
py -3 .bld\test_modules.py     # 模块功能（配置/存储/日志/OCR/搜索/更新）
py -3 .bld\test_api.py         # 接口（需先启动服务）
py -3 .bld\test_channels.py    # 稳定/测试双通道
py -3 .bld\test_ocr.py         # OCR
py -3 eval\harness.py --all --seed 20260101   # 联网搜索评测
```

## 第五条：分支约定

| 分支 | 用途 |
| --- | --- |
| `main` | **始终是可发布的稳定版**；只有从 `release/*` 合回来才动 |
| `dev` | 日常开发推这里 |
| `release/<版本>` | 准备发版时从 `dev` 切出，只修 bug |

## 第六条：改动前先读 `PLAN.md`

`PLAN.md` 是项目计划与约定（**不可删除**），里面写清了架构、各模块设计取舍、
以及「版本管理约定」。改任何模块前先看对应章节，避免重复踩坑。

---

## 发版标准流程（仅在获得用户同意后执行）

```powershell
# 0) 先问用户："这次要发版吗？建议版本号 X.Y.Z，可以吗？"  ← 必须做
# 1) 用户同意后：
py release.py                          # 查看以 git 标签为基准算出的下一个号
py release.py --approved 1.1.1 --release   # 一键：写版本号 → 打标签 → 推送 → 建 Release
py release.py --approved 1.2.0-beta.1 --release --beta   # 发测试版
```

> `--approved <版本号>` 是**人工同意凭据**：不提供它，涉及改版本号的命令会直接拒绝执行。
> 这是刻意设计的摩擦，用来强制"先问人"。

---

## 稳定版 / 测试版

- 正式版：`1.2.0`；测试版：`1.2.0-beta.1`（带 `-beta`/`-rc`/`-alpha` 即预发布）。
- GitHub 上把测试版 Release 勾成 **pre-release**。
- 程序里 `update_channel` 默认 `stable`，**不给普通用户提示预发布版**
  （实测不加这道闸，一发测试版所有用户都会被提示更新）。
